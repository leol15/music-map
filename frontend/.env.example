# URL of your backend (via CloudFront), no trailing slash
# e.g. https://abc123.cloudfront.net/api/music-map
VITE_API_BASE_URL=https://your-cloudfront-domain/api/music-map

# GitHub Pages base path — must match your repo name, e.g. /music-map/
VITE_BASE_PATH=/music-map/
